/**
 * Implement promiseAll here.
 * See README.md for the full problem description.
 */

export function promiseAll(promises: any[]): Promise<any[]> {
  // TODO: Implement this
  return Promise.resolve([]);
}
